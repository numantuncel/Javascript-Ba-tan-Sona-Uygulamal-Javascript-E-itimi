const hata = document.querySelector("p"); // tek p yi seçer
const error = document.querySelector(".error"); // classı erorr olan

console.log(hata); // ilk class erorr olanı seçer

// div in içinde clası erorr olan

const Diverror = document.querySelector("div.error");

// hepsini seçmek için

const hatalar = document.querySelectorAll("p");

console.log(hatalar)
console.log(hatalar[0])
